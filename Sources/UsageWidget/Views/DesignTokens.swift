import SwiftUI

/// Shared design tokens for plan cards, progress bars, and states (Phase 2+).
enum DesignTokens {
    static let cornerRadius: CGFloat = 10
    static let cardSpacing: CGFloat = 12
    static let popoverWidth: CGFloat = 312
}
