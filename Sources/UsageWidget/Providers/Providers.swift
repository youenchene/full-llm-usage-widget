// Providers/
//
// Phase 2 (Claude, Codex, Copilot, Mistral) and Phase 3 (Gemini, Scaleway,
// OpenCode Go + Zen) add one module per provider in this folder. Each conforms to
// `UsageProvider` (Domain/) and returns a normalized `ProviderUsage`.
// Nothing is wired here in Phase 1.
