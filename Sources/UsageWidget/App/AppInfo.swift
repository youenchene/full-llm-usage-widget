import Foundation

/// App-level constants shared across the packaged `.app` and the SPM executable.
enum AppInfo {
    static let bundleIdentifier = "com.micrix.full-llm-usage-widget"
    static let appName = "Full LLM Usage Widget"
}
